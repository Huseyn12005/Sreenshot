<Window x:Class="ClientSide.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientSide"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Image x:Name="ImageShare"/>
        </Grid>
        <Button Grid.Row="1" Height="40" Width="120" x:Name="button" Content="Start" Foreground="White" FontSize="18"
            Background="Green"   Command="{Binding StartCommand}" HorizontalAlignment="Center" Margin="0,0,0,10" />

    </Grid>
</Window>
